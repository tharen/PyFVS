language: python

python:
  - "3.5"

addons:
  apt:
    packages:
    - cmake-2.8.12
    - gfortran
    - unixodbc
    - unixodbc-dev
    
script:
  - bash ./build_pyfvs.sh
  - make

branches:
  only:
    - module_refactor
